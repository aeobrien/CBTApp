// DesignSystem: reusable UI components and design tokens.
// Depends on Domain for model types used in component interfaces.

import Domain

/// Namespace for the DesignSystem layer.
public enum DesignSystemModule {
    public static let version = "0.1.0"
}
