// Data layer: SwiftData persistence and repository implementations.
// Depends on Domain for entity types and repository protocols.

import Domain

/// Namespace for the Data layer.
public enum DataModule {
    public static let version = "0.1.0"
}
