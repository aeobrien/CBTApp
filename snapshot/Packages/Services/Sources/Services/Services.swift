// Services layer: agent service, validation pipeline, safety system.
// Depends on Domain for entity types and Utilities for logging.

import Domain
import Utilities

/// Namespace for the Services layer.
public enum ServicesModule {
    public static let version = "0.1.0"
}
